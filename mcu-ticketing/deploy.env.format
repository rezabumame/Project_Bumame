# GCP
GCP_PROJECT=your-gcp-project-id
DEPLOY_REGION=asia-southeast2
SERVICE_NAME=mcu-ticketing

# Artifact Registry
AR_HOSTNAME=asia-southeast2-docker.pkg.dev
AR_PROJECT_ID=your-gcp-project-id
AR_IMAGE_PATH=cloud-run-source-deploy/mcu-ticketing
IMAGE_TAG=latest

# Full image (do not edit; used by scripts)
# IMAGE=${AR_HOSTNAME}/${AR_PROJECT_ID}/${AR_IMAGE_PATH}:${IMAGE_TAG}
